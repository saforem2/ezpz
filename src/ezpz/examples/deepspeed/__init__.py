"""DeepSpeed-oriented training utilities for ezpz examples.

Launch a specific script with:

    ezpz launch -m ezpz.examples.deepspeed.<module> [args...]

Package init is not a CLI, so ``python3 -m ezpz.examples.deepspeed`` does not
offer ``--help`` output on its own.
"""
