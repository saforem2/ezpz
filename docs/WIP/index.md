# 🍋 `ezpz` (WIP Docs Hub)

- For a uv-first quickstart, see `WIP/quickstart.md`.
- For component flow, see `WIP/architecture.md`.
- For CLI flags and env vars, see `WIP/launch.md`.
- For system-specific guidance, see `WIP/systems.md`.
- For troubleshooting, see `WIP/faq.md`.
- For tests overview, see `WIP/tests/index.md`.
- For API/entrypoint cheatsheet, see `WIP/Code-Reference/cheatsheet.md`.
- For release/publishing steps, see `WIP/release.md`.
- For the draft public README, see `WIP/readme-draft.md`.

## Build/Serve locally (WIP)
```bash
uv pip install -e ".[docs]"   # once, to get mkdocs + plugins
uv run mkdocs serve
# or build the static site
uv run mkdocs build
```
